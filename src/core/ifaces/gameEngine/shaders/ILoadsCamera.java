package core.ifaces.gameEngine.shaders;


import core.utils.Camera;

/**
 * @author storm
 *
 */
public interface ILoadsCamera {

	void loadCamera(Camera camera);
	// protected Matrix4f createViewMatrix(Camera camera);
	// protected void loadViewMatrix(Matrix4f viewMatrix);

}
